# RGB-D 深度融合评审反馈总结

## 1. 反馈范围

本文对 `DEPTH_FUSION_REVIEW.md` 中 P0-P5 的判断进行复核，依据包括：

- 当前 RGB-D 数据、模型和训练调用链；
- `mergevla` Conda 环境中的现有测试；
- 当前 bf16 autocast 路径下的模块级数值探针；
- RoboTwin 深度生成代码中的单位、无效值和相机远平面语义。

本次仅做分析和验证，没有修改模型、数据或训练代码。

## 2. 总体结论

原评审是一份有价值的风险清单，但不宜直接作为修改计划执行。其内容可分为三类：

1. **已经由代码证实、建议保留的问题**：深度单位校验、RGB/深度相机顺序校验、部分无效像素缺少显式 validity 表征、`learned_patch` 路径测试缺失。
2. **合理但尚未证实的风险**：center crop、超过 5 米深度的处理、分视角归一化、位置编码强度、深度编码器容量。
3. **需要修正的技术结论**：P2 所称的 bf16 门控死区、P3 所称的位置参数从初始化 checkpoint 加载、P4 所称的当前编码器无法表达边缘和表面方向。

建议先完成低成本防误配检查和数据/训练监控，再根据统计与消融实验决定是否改变表征结构。当前证据不足以支持立即修改 `gate_init`、分视角归一化、位置编码或编码器深度。

## 3. 逐项复核结果

| 原评审项目 | 复核判断 | 建议处理 |
| --- | --- | --- |
| P0.1 深度单位不校验 | 成立，但属于潜在误配风险，不是当前毫米数据链路中的已发生 bug | 保留，增加显式校验 |
| P0.2 RGB center crop | 风险描述部分成立，但“当前会静默错位”的严重性被夸大 | 锁定处理器配置并补尺寸检查，不应先认定已有错位 |
| P0.3 相机顺序只靠约定 | 成立 | 保留，比较规范化后的相机 ID |
| P1.1 无效像素填成中等距离 | 成立，是当前最明确的表征问题 | 优先统计缺失比例，再评估 validity 通道 |
| P1.2 超过 5 米被视为无效 | 是设计取舍，不能直接定性为错误 | 先统计各相机 `>5m` 比例并做消融 |
| P1.3 三视角共享归一化 | 证据不足，且原文比例计算有误 | 保持共享米制语义作为基线，先统计分布 |
| P2 gate 与 bf16 | 第 0 步梯度分析正确；bf16 死区结论错误 | 保留 gate 监控，删除固定 `4e-3` 门槛结论 |
| P3 初始化时位置盲 | 小位置项是事实；退化结论尚未测量；checkpoint 加载描述错误 | 改写为待验证假设并增加注意力诊断 |
| P4 深度编码器太浅 | 缺少跨 patch 上下文成立；“无非线性、不能表达边缘”不成立 | 降级为架构消融，不作为必改项 |
| P5 工程问题 | 大部分成立，但严重性较低 | 分别作为可维护性、性能和测试任务处理 |

## 4. 已确认应保留的问题

### 4.1 数据单位与模型配置缺少闭环校验

`datasets.py` 会把 `depth_unit` 写入数据集元数据，`MetricDepthEncoder` 则只根据 `config.depth.input_unit` 决定是否除以 `depth_scale`，二者之间没有校验。

因此，若数据实际或声明为米，而模型配置仍为毫米，`0.5-5.0m` 会被解释为 `0.0005-0.005m`，随后全部低于 `min_depth_m=0.05` 并进入无效 mask。训练可以继续运行，但深度残差会长期为零。

当前官方链路不存在该误配：

- RoboTwin `camera.py` 将米乘以 1000 后保存为 `uint16` 毫米；
- 数据附加脚本声明 `depth_unit: millimeter`；
- 评估接口也把深度转换为 `uint16` 毫米。

所以它应被描述为“高价值防误配断言”，而不是当前数据已经错误。

### 4.2 RGB 与深度相机顺序应显式验证

当前训练配置中两组顺序一致：

```text
RGB:   cam_high, cam_left_wrist, cam_right_wrist
depth: cam_high, cam_left_wrist, cam_right_wrist
```

评估代码也按 `[head, left, right]` 组装 RGB 和深度。但模型接收的只是两个有序张量，无法识别左右腕是否被交换，因此增加相机 ID 和列表长度校验是合理的。

建议比较规范化后的完整相机 ID，不要只依赖容易碰撞的字符串尾部。

### 4.3 部分无效 patch 缺少 validity 语义

`MetricDepthEncoder` 在 log 归一化后把无效像素填为 `0.0`。在当前 `min=0.05m`、`max=5m` 配置下，归一化值 `0.0` 对应几何中点约 `0.5m`。

当一个 patch 的无效比例低于 `invalid_threshold=0.5` 时，该 token 仍然参与 K/V；卷积无法区分其中的 `0.0` 是“未知像素”还是“真实约 0.5m”。这是实际存在的信息歧义。

增加 validity 通道是合理方案之一，但不是唯一方案。还可以比较：

- validity 通道；
- 额外的 patch 有效比例特征；
- mask-aware pooling/convolution；
- 更严格的 token 屏蔽阈值。

在改变 `patch_embed.weight` 形状前，应先统计三个相机的像素缺失率和 patch 缺失率。

## 5. 需要降级为条件风险的问题

### 5.1 center crop 尚不能定性为当前 P0 bug

运行时只修改了 `image_processor.size`，没有显式设置 `crop_size` 或 `do_center_crop`，因此预处理配置确实不够封闭。

但当前训练与评估链路还有两层保护：

- 训练样本在进入处理器前已经被 resize 为 `224x224` 方形图；
- 评估 RGB 和深度也会先 resize 到模型目标尺寸；
- RoboTwin 模型使用 `learned_patch`，处理器若产生不同 patch 数，`_position_visual_tokens` 会因位置长度不匹配而报错。

因此，“当前 RGB 必然被裁剪并静默破坏一一对应”没有得到证实。更准确的表述应是：处理器空间变换依赖外部配置，需要显式锁定并测试。

由于当前环境没有设置实际 `DINOV3_MODEL_PATH`，且公开模型仓库受访问限制，本次无法读取训练所用本地处理器的最终 `crop_size/do_center_crop`。这项检查仍应在实际训练机器上完成。

### 5.2 超过 5 米是否保留属于任务设计取舍

RoboTwin 相机近远平面为 `0.1m/100m`，所以大于 5 米但有限的数值可以是真实观测。当前编码器会把这部分像素判为无效。

不过，对机械臂操作任务而言，主动屏蔽远背景也可能有益：它减少无关 K/V，避免大量远平面 token 稀释工作区深度信息。因此不能只根据传感器语义断言“超过 5 米必须 clamp 后保留”。

需要先统计：

- 各相机零值、非有限值比例；
- 各相机 `>5m` 像素和 patch 比例；
- 被屏蔽区域是否包含任务相关物体；
- 保留远背景对成功率和注意力分布的影响。

### 5.3 分视角归一化不一定优于共享米制归一化

原评审称 `0.05-0.5m` 占约 55% 的 log 动态范围。实际为：

```text
log(0.5 / 0.05) / log(5 / 0.05) = log(10) / log(100) = 50%
```

`0.8-2.0m` 约占总 log 范围的 20%，不能仅据此认定 head camera 被严重压缩。

共享归一化还保留了重要物理先验：同一个归一化值在三个相机中代表同一米制距离。分视角 min/max 会破坏这个语义，并让共享编码器面对不同的数值标尺。

如果统计显示分布差异很大，可优先研究 view-conditioned affine/encoder，而不是直接为每个视角使用不同的物理范围。

## 6. P2：bf16 门控结论需要重写

### 6.1 正确部分

`gate_init=0.0` 时：

- `tanh(0)=0`；
- 第 0 步深度编码器和交叉注意力参数的梯度为零；
- `depth_gate` 可以获得非零梯度；
- gate 离开零点后，梯度才进入深度编码器和注意力参数。

这是零初始化残差门控的预期行为，也由现有测试覆盖。

### 6.2 原评审中不成立的部分

原评审根据 bf16 在 1 附近的量化精度，推断 `gate < 4e-3` 时：

```text
fused = rgb + gate * delta
```

会完全舍入为原 RGB，前向逐位不变。

当前代码路径并非如此。训练器和 interaction 使用的是 autocast，DeepSpeed 配置没有把整个模型权重强制转换为 bf16。按现有路径运行模块级探针得到：

```text
VisionProjection 输出: float32
depth token:           float32
fusion 输出:           float32
```

在同一 autocast 上下文中，数值结果为：

| `depth_gate` 参数值 | 与零 gate 输出逐位相同 | 变化元素比例 | 最大绝对变化 |
| ---: | --- | ---: | ---: |
| `1e-4` | 否 | 约 99.93% | 约 `8.5e-5` |
| `5e-4` | 否 | 约 99.98% | 约 `4.3e-4` |
| `1e-3` | 否 | 约 99.99% | 约 `8.5e-4` |
| `4e-3` | 否 | 约 99.998% | 约 `3.4e-3` |

这足以排除“低于约 `4e-3` 时前向完全不敏感”的固定门槛结论。

### 6.3 建议保留和删除的内容

建议保留：

- 记录 `tanh(depth_gate).abs().mean()`、最大值和分位数；
- 记录深度编码器/交叉注意力梯度范数；
- 用短程玩具过拟合确认 gate 能离开零点；
- 对比 `gate_init=0` 与小非零初始化。

建议删除或改为消融假设：

- 固定的 `4e-3` bf16 死区；
- 直接把默认 `gate_init` 改成 `0.05-0.1`；
- 把 post-fusion LayerNorm 称为无代价保险；
- 无依据地给 `delta` 乘 `1/sqrt(d)`。

post-fusion LayerNorm 会在 gate 为零时仍改变 RGB token，从而破坏与 RGB-only checkpoint 的严格等价性。MHA 内部也已经包含标准的注意力缩放，额外的 `1/sqrt(d)` 需要实验依据。

## 7. P3：位置编码风险合理，但论证需要修正

### 7.1 可以确认的事实

当前额外 patch 位置项的初始量级约为：

```text
patch_position_embedding std = 0.01
patch_position_scale         = 0.01
乘积 std                     ≈ 1e-4
```

而经过 LayerNorm 的 RGB/depth 内容 token 每通道标准差约为 1。因此，这个额外位置扰动在初始化时确实很弱，难以单独提供强对角匹配先验。

### 7.2 checkpoint 加载描述错误

原评审称位置参数“从预训练 RGB checkpoint 加载”。当前 `_load_initialization` 只映射：

- BERT；
- 文本投影；
- interaction 的文本层和融合层。

它不映射 `patch_position_embedding`、`patch_position_scale` 或 `view_embedding`。因此，在普通 `TURBOVLA_INIT_CKPT` 初始化流程中，这些位置参数是当前模型新建的参数，不需要到 legacy checkpoint 中查找其数值。

只有恢复一个完整 TurboVLA 训练 checkpoint 时，位置参数才可能从 checkpoint 恢复。

### 7.3 尚未证实的推断

以下结论不能仅由位置项较小推出：

- 注意力必然近似均匀；
- 每个 RGB Query 得到几乎相同的深度摘要；
- `delta` 已经退化成全图平均池化；
- 当前机制几乎无法学习空间对应。

RGB Query 来自 DINOv3，本身已经包含位置与全局上下文；随机初始化的 Q/K 投影也会让不同 Query 产生不同权重。小位置项说明对角先验弱，但不能证明输出相同。

建议实际记录：

- 每个 head 的注意力熵；
- 对角线及局部窗口的注意力质量；
- 不同 RGB Query 之间注意力分布的差异；
- 初始化、短训练和收敛后的变化；
- 无位置、当前位置、局部 bias 和局部/全局混合方案的成功率。

全局同视角注意力也有实际优点：能容忍 RGB-D 小幅错位、利用对应位置之外的有效深度、获得物体级和关系型上下文。局部路径更强的几何先验与全局路径的鲁棒性应通过混合方案做消融，而不是预先认定全局方案无效。

## 8. P4：编码器容量问题被过度表述

当前深度编码器确实只有一个 patch 卷积，没有 depth token 之间的 self-attention，因此每个 Key 在进入 cross-attention 前缺少跨 patch 上下文。

但以下描述不准确：

1. 整个编码器不是“没有非线性”。原始深度先经过可选 log 变换，卷积后还有 LayerNorm。
2. 线性卷积可以表达边缘、梯度、局部斜率和像素差。经典边缘算子本身就是线性滤波器。
3. 当前 `16x16 -> 256` 投影的输入和输出维度都是 256，理论上可以保留大量 patch 内部信息，而不只是“平均距离”。

真正明确的限制是：

- patch 之间没有编码器上下文；
- 非重叠 patch 对跨边界结构不敏感；
- validity 信息存在歧义；
- 深度分支完全从随机初始化开始学习跨模态匹配。

MLP、卷积 stem、自注意力、深度梯度或法向输入都可以作为消融项，但目前没有证据证明它们是训练前必须实施的 P4 修复。

## 9. P5 工程项复核

以下问题成立：

- `DepthEncoderConfig` 默认 `224/3 views`，`VisionEncoderConfig` 默认 `256/2 views`，启用默认深度配置时会冲突；
- image/patch 一致性检查发生在 DINOv3 构建之后，失败时间偏晚；
- 调用方和 fusion 内部存在重复 `.to(...)`；
- `flat_depth.clone()` 有额外显存和带宽成本；
- CLI recipe 默认值从 RGB 配方变成深度配方，会改变不带参数时的行为；
- 现有完整前向测试使用默认 `position_embedding="view"`，没有覆盖 RoboTwin 实际的 `learned_patch` 路径。

需要注意：

- patch size 来自实际 DINOv3 配置，若想在加载权重前验证，必须给 vision config 增加显式 patch size，或先只加载轻量配置；不能简单把现有检查原样移动到 dataclass。
- `clone()` 是性能问题，不是正确性问题；是否优化应通过显存和吞吐 profiling 决定。
- 默认使用 RGB 还是 RGB-D 配方属于入口兼容性决策，需要与项目预期保持一致。

## 10. 建议的修订优先级

### 第一阶段：低风险工程加固

1. 校验数据集声明单位与模型 `input_unit`。
2. 校验 RGB/depth 相机 ID、数量和顺序。
3. 显式锁定图像处理器的 resize/crop 行为，并检查最终 `H x W`。
4. 增加 `learned_patch` RGB-D 完整前向测试。
5. 保留 `gate_init=0` 的 RGB 等价性测试。

### 第二阶段：先取得数据和训练证据

1. 统计三个相机的深度直方图、零值、非有限值、`>5m` 比例。
2. 统计像素级和 patch 级无效比例。
3. 记录 gate、深度分支梯度范数和 `delta` 范数。
4. 记录注意力熵、对角质量和局部窗口质量。
5. 做一个小数据集玩具过拟合，确认深度分支能被真正启用。

### 第三阶段：最小消融

1. 无 validity 通道 vs validity 通道。
2. `gate_init=0` vs 小非零值；不预设 bf16 门槛。
3. 当前全局注意力 vs 局部窗口 vs 对角残差 + 全局注意力。
4. `>5m` 屏蔽 vs clamp 后保留。
5. 共享归一化 vs view-conditioned 方案。

### 第四阶段：有证据后再增加容量

只有当注意力诊断和任务指标表明深度 Key/Value 表征不足时，再考虑：

- 非线性 MLP 或卷积 stem；
- 跨 depth token self-attention；
- 相对位置 bias；
- 深度梯度、法向或其他几何通道。

## 11. 建议对原评审的核心改写

建议把原来的总体判断：

> 当前机制在初始化状态下几乎无法建立空间对应。

改为：

> 当前额外 patch 位置项初始幅度较小，融合模块缺少显式局部/对角几何先验，因此初始化时的空间对应强度存在不确定性。需要通过注意力熵、对角质量、局部质量以及短程训练实验确认它是否能够学习稳定的 RGB-D 对应关系。

建议把 P2 的结论改为：

> 零门控会让第 0 步只有 gate 获得非零梯度，这是预期的兼容性设计。当前 autocast 路径下未观察到固定的 bf16 前向死区；是否需要非零初始化应根据 gate/梯度监控和短程消融决定。

建议把 P4 的结论改为：

> 当前编码器能够表达 patch 内的线性几何模式，但缺少显式 validity 语义、跨 patch 上下文和更强的非线性建模。是否增加容量应由注意力诊断和任务消融决定。

## 12. 最终建议

原评审中最值得立即保留的是“防静默误配”和“补监控/测试”的方向。架构修改部分应从确定性修复调整为分阶段实验假设，尤其不建议在没有数据统计和短程消融前同时修改 validity、远距离语义、gate 初始化、位置编码和编码器结构，否则后续无法判断性能变化来自哪一项。
